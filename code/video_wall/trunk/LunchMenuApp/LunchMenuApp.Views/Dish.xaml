<UserControl x:Class="LunchMenuApp.Views.Dish"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:LunchMenuApp.ViewModels;assembly=LunchMenuApp.ViewModels" mc:Ignorable="d"
             d:DataContext="{d:DesignInstance ViewModels:DishViewModel}">
    <StackPanel Margin="10 0">
        <TextBlock Text="{Binding Type}" TextAlignment="Center" FontSize="25" FontFamily="Arial" FontWeight="Bold"  Margin="0,0,0,5" />
        <TextBlock Text="{Binding Name}" TextAlignment="Center" FontSize="20" FontFamily="Arial" LineHeight="25" />
        <TextBlock Text="{Binding Price}" TextAlignment="Center" Foreground="Gray" FontSize="20" FontFamily="Arial" Margin="0,5,0,0" />
    </StackPanel>
</UserControl>