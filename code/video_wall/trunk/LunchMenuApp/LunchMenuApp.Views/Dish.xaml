<UserControl x:Class="LunchMenuApp.Views.Dish"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ViewModels="clr-namespace:LunchMenuApp.ViewModels;assembly=LunchMenuApp.ViewModels" mc:Ignorable="d"
             d:DataContext="{d:DesignInstance ViewModels:DishViewModel}">
    <StackPanel Margin="10 0">
        <TextBlock Text="{Binding Type}" TextAlignment="Center" FontSize="25"  FontWeight="Bold"  Margin="0,0,0,7" />
        <TextBlock Text="{Binding Name}" TextAlignment="Center" LineHeight="27" />
        <TextBlock Text="{Binding Price}" TextAlignment="Center" Foreground="Gray" Margin="0,5,0,0" />
    </StackPanel>
</UserControl>